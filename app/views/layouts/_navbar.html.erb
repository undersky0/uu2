    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Undersky</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Home", ""  %></li>
              <li><%= link_to "My Account", ""  %></li>
              <li><%= link_to "Settings", ""  %></li>
              
              
              <% if  user_signed_in? %>
              Singed in as <%= current_user.email %>. Not you ? <%= button_to('Logout', destroy_user_session_path, :method => :delete) %>
              <%= current_user.id%><%= current_user.full_name%> <%= current_user.profile.firstname%>
              <% else %>
              <li><%= link_to('Register', new_user_registration_path)  %></li>
              <li><%= link_to('Sign In', new_user_session_path )%></li>
              <% end %>
              
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>